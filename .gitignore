.build/
.swiftpm/
# scripts/convert-nb-whisper.sh writes its venv and ~1 GB of CoreML artifacts
# here. Never belongs in the repo.
build/
*.xcodeproj
*.xcworkspace
.DS_Store

# kdb auto-rendered task index (regenerate with `kdb render`)
.tasks/
